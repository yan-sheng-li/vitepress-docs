#  Win 批处理基础入门知识

------

## 一、什么是 BAT 脚本？

`.bat` 是 **Windows 批处理文件（Batch File）**，本质上是把多条 **CMD 命令**写在一个文本文件里顺序执行。

创建方法：

1. 新建文本文件
2. 改名为：`xxx.bat`
3. 双击即可运行

------

## 二、基础语法结构

#### 1️⃣ 注释

```bat
rem 这是注释
:: 这也是注释
```

------

#### 2️⃣ 关闭命令回显

```bat
@echo off
```

- `echo off`：关闭命令显示
- `@`：表示当前行不显示

👉 基本所有 bat 文件第一行都会写：

```bat
@echo off
```

------

#### 3️⃣ 输出内容

```bat
echo Hello World
```

输出空行：

```bat
echo.
```

------

#### 4️⃣ 暂停

```bat
pause
```

效果：显示

```
Press any key to continue . . .
```

------

## 三、变量使用

#### 1️⃣ 定义变量

```bat
set name=Tom
echo %name%
```

⚠ 注意：

- 等号两边不要有空格
- 使用变量时要加 `%`

------

#### 2️⃣ 用户输入

```bat
set /p name=请输入名字：
echo 你好 %name%
```

------

#### 3️⃣ 计算（简单整数运算）

```bat
set /a num=1+2
echo %num%
```

支持：`+ - * / %`

------

## 四、常用命令

| 命令 | 作用       |
| ---- | ---------- |
| dir  | 查看目录   |
| cd   | 切换目录   |
| md   | 创建文件夹 |
| rd   | 删除文件夹 |
| del  | 删除文件   |
| copy | 复制文件   |
| move | 移动文件   |
| cls  | 清屏       |
| exit | 退出       |

示例：

```bat
md test
cd test
echo hello > a.txt
```

------

## 五、条件判断（if）

#### 1️⃣ 字符串判断

```bat
if "%name%"=="Tom" (
    echo 是Tom
) else (
    echo 不是Tom
)
```

⚠ 字符串比较建议加双引号，避免空值报错。

------

#### 2️⃣ 数值判断

```bat
if 10 GTR 5 echo 10大于5
```

比较符：

| 关键字 | 含义     |
| ------ | -------- |
| EQU    | 等于     |
| NEQ    | 不等于   |
| LSS    | 小于     |
| LEQ    | 小于等于 |
| GTR    | 大于     |
| GEQ    | 大于等于 |

------

## 六、循环（for）

#### 1️⃣ 遍历文件

```bat
for %%i in (*.txt) do (
    echo %%i
)
```

⚠ 在 bat 文件中用 `%%i`
⚠ 在命令行中用 `%i`

------

#### 2️⃣ 数字循环

```bat
for /l %%i in (1,1,5) do (
    echo %%i
)
```

格式说明：

```
(开始,步长,结束)
```

------

## 七、goto 跳转

```bat
@echo off
:menu
echo 1. 选项1
echo 2. 退出
set /p choice=请选择：

if "%choice%"=="1" goto menu
if "%choice%"=="2" exit
```

`:menu` 是标签

------

## 八、调用其他程序

运行 exe：

```bat
start notepad.exe
```

调用其他 bat：

```bat
call other.bat
```

⚠ 如果不用 `call`，当前脚本会被替换掉

------

## 九、文件重定向

覆盖写入：

```bat
echo hello > a.txt
```

追加写入：

```bat
echo world >> a.txt
```

------

## 十、常见进阶知识

| 知识点     | 说明                            |
| ---------- | ------------------------------- |
| 延迟变量   | setlocal enabledelayedexpansion |
| 错误码     | %errorlevel%                    |
| 管道符     | `                               |
| && 和      |                                 |
| 批处理参数 | %1 %2 %3                        |

示例（获取参数）：

```bat
echo 第一个参数是 %1
```

运行：

```
test.bat abc
```

输出：

```
第一个参数是 abc
```

------

## 十一、一个完整示例

```bat
@echo off
set /p name=请输入名字：
if "%name%"=="" (
    echo 不能为空
    pause
    exit
)

echo 你好 %name%
pause
```

------

